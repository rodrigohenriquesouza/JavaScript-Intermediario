function validarIdade(v) {

    if(v >= 18) {
        console.log('Você pode entrar na auto-escola.')
    } else {
        console.log('Você não tem idade para entrar na auto-escola.')
    }
}

validarIdade(19)
validarIdade(22)
validarIdade(15)
validarIdade(14)
validarIdade(18)
